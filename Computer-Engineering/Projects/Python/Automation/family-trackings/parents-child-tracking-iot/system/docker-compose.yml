version: '3.8'

services:
  family-tracker-api:
    build: .
    container_name: family_tracker_engine
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - ENV=development
    command: python tools/device_simulator.py
