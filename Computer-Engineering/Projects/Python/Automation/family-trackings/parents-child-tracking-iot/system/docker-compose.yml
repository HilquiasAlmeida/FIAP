version: '3.8'

services:
  family-tracker-api:
    build: .
    container_name: family_tracker_core
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    command: uvicorn api.endpoints:app --host 0.0.0.0 --port 8000 --reload
